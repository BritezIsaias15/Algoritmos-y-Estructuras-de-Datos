# Consigna

Escribir una función/método llamado ordenar que toma un arreglo de enteros. El método debe devolver el mismo arreglo que tomó como parámetro, pero con sus valores ordenados de manera ascendente.

