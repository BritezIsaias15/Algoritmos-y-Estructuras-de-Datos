# Consigna

Escribir una función/método llamado mezclarArreglos que toma 2 arreglos de enteros del mismo tamaño y devuelve un nuevo arreglo del mismo tamaño que el de los de entrada que contiene los elementos de posiciones impares del primer arreglo y los de posiciones pares del segundo arreglo.
